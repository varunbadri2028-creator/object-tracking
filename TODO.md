- [x] Inspect repository structure
- [x] Identify non-essential files for GitHub readiness
- [ ] Remove local virtual environment directory (venv)
- [ ] Add .gitignore to exclude venv, DS_Store, and caches
- [ ] (Optional) Pin dependency versions in requirements.txt for reproducibility
- [ ] Validate project runs from fresh install (pip install -r requirements.txt)
- [ ] Ensure only mandatory files remain: README.md, requirements.txt, tracker.py (+ optional weights if desired)

